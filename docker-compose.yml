services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped

  frontend:
    build: ./ui/frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
